# Copy this file to .env and fill in real values (do NOT commit secrets).
# Example .env (placeholders only)

PORT=8081

# MongoDB connection URI
# Format: mongodb+srv://<username>:<password>@<host>/<dbname>?retryWrites=true&w=majority
DB_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/<dbname>?retryWrites=true&w=majority

# JWT keys
JWT_ACCESS_KEY=<your_jwt_access_key_here>
JWT_REFRESH_KEY=<your_jwt_refresh_key_here>
JWT_RESET_PASSWORD_KEY=<your_jwt_reset_password_key_here>
JWT_CHANGE_EMAIL_KEY=<your_jwt_change_email_key_here>

# Google OAuth (if used)
CLIENT_ID=<google_oauth_client_id>
CLIENT_SECRET=<google_oauth_client_secret>

# Email account for sending (use app password if required)
EMAIL_USER=your-email@example.com
EMAIL_PASS=your-email-password-or-app-password

# Cloudinary (image hosting)
CLOUD_NAME=<cloudinary_cloud_name>
CLOUD_API_KEY=<cloudinary_api_key>
CLOUD_API_SECRET=<cloudinary_api_secret>

# Notes:
# - Keep actual .env out of version control (add to .gitignore).
# - Replace angle-bracket placeholders with real values.
# - Avoid committing any keys or passwords to the repo.
